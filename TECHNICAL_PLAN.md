# 角斗士棋在线版技术规划

## 当前交付

- 2–4 人私人房间，创建时确定人数、房间容量和棋盘尺寸。
- 六位房间号加入；房间满员或已经开局时拒绝新玩家。
- 房主必须等所有座位坐满后才能开始。
- Python 服务端权威校验回合、棋块、边界、重叠、同色边接和对角接触。
- SSE 长连接向所有浏览器推送同一房间状态，断线后自动重连。
- 玩家身份令牌只保存在浏览器本地，不出现在邀请 URL。
- 2/3/4 人分别使用 14×14、17×17、20×20 棋盘，使最大填充率稳定在约 89%–92%。
- 玩家分数从原左侧大面板改为棋盘上方紧凑状态条。

## 核心架构

### 浏览器

- 原生 HTML、CSS、JavaScript，无构建步骤。
- `shared/pieces.json` 是前后端共用的 21 种棋块定义。
- 客户端负责选择、旋转、翻面和合法位置预览。
- 客户端发送 `expectedVersion`，避免基于过期棋盘落子。
- 服务端响应和 SSE 事件是最终状态来源。

### 服务端

- Python 3.10 标准库 `ThreadingHTTPServer`，不增加服务器依赖。
- 房间状态保存在进程内存中，等待房间 2 小时、结束房间 1 小时后自动清理。
- 请求体限制为 16KB，并按来源 IP 做基础频率限制。
- 玩家使用随机 ID 与随机令牌恢复房间身份。
- 房间服务只监听 `127.0.0.1:8790`，不直接暴露公网端口。

### Nginx

- `/blokus/` 从 `/var/www/html/blokus` 提供静态资源。
- `/blokus/api/` 代理到 `127.0.0.1:8790/api/`。
- SSE 关闭代理缓存并使用一小时读取超时。

## 数据与协议

房间状态：

- `waiting`：等待玩家，允许加入。
- `playing`：游戏进行中，禁止新玩家加入。
- `finished`：完成结算，禁止落子。

主要接口：

- `POST /api/rooms`：创建房间。
- `POST /api/rooms/{code}/join`：加入房间。
- `POST /api/rooms/{code}/start`：房主开始游戏。
- `POST /api/rooms/{code}/place`：提交落子。
- `POST /api/rooms/{code}/finish`：服务端确认无合法落点后结束该玩家。
- `POST /api/rooms/{code}/leave`：离开或认输。
- `GET /api/rooms/{code}/events`：SSE 实时状态。

## 规则边界

当前在线模式是“每名玩家控制一种颜色”的房间变体：

- 2 人：14×14，蓝方与红方使用对角起点。
- 3 人：17×17，蓝方、黄方与红方使用三个角。
- 4 人：20×20，蓝、黄、红、绿四方。

玩家关闭页面只会被标记为离线；服务端不会自动判负。玩家点击“无合法落点，确认结束”且通过合法落点扫描后，才会退出后续回合。其他玩家继续游戏，所有玩家均结束后展示最终排名。

官方两人模式常见规则是一人控制两种颜色；如需严格官方玩法，应作为独立模式增加，不能与当前房间模式共用结算语义。

## 部署信息

- 云服务：腾讯云 Lighthouse，Ubuntu 22.04。
- 静态目录：`/var/www/html/blokus`。
- 应用目录：`/opt/blokus`。
- systemd：`blokus.service`。
- 本机监听：`127.0.0.1:8790`。

私钥、密码和其他凭据不写入仓库。

## 后续演进

1. 首版观察真实房间数量、并发连接和断线率。
2. 需要跨重启恢复时，把房间事件写入 SQLite 或 PostgreSQL。
3. 需要多进程或多机器时，引入 Redis Pub/Sub 和共享房间状态。
4. 增加官方双人模式、观战、回放、计时器和 AI 玩家。
5. 将规则引擎进一步生成跨语言测试向量，保证 Python 服务端和 JavaScript 预览结果一致。

## 验收

- Python 规则与 API 自动化测试。
- 两个以上独立浏览器会话创建、加入、开局和交替落子。
- 第 `capacity + 1` 名玩家收到 `ROOM_FULL`。
- iPad 竖屏 `768×1024`、横屏 `1024×768` 和桌面视口无横向溢出。
- 公网页面、健康接口、SSE、systemd 和 Nginx 均正常。
