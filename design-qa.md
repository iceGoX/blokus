# UI 验证记录

日期：2026-09-08。范围：角域奶油拼趣 UI 改动；本地验证，不代表已部署或公网验收。

## 设计与证据

- source visual truth：[Pixso 设计文件](https://pixso.net/app/design/EkiwN1Njlbxn043HnBJpVA)；已确认概念图中的已知错误以 AGENTS.md 的规则约束校正。
- implementation screenshots：[手机大厅](docs/design/mobile-entry.png)、[桌面对局](docs/design/desktop-game.png)。
- full-view comparisons：[大厅并排对照](docs/design/entry-comparison.png)、[对局并排对照](docs/design/game-comparison.png)。
- 视口：手机390×844、平板834×1194、桌面1440×1024；另有桌面1280宽初检。
- 像素归一化：Pixso 大厅总板1800×1170，从中裁出390宽的单屏内容；浏览器手机截图为390×844。桌面截图1425×1013，对应1440×1024 CSS视口；并排图按宽度等比缩放，未改变棋盘比例。
- 状态：大厅创建；对局为真实本地局面。Pixso示例玩家、棋子数量与在线标记可能不同，未用这些数据差异判断视觉回归；对局对照用于检查配色、布局分区和操作区，不宣称跨视口逐像素一致。
- focused region comparison：在同一并排输入中查看大厅表单、主按钮、文字、插画边缘与棋盘/操作区。不是只凭源码或单张截图判定。

## 检查结果

| 检查面 | 结果 |
| --- | --- |
| 字体与层级 | 宋体标题、清晰正文；保留真实状态标题和说明，系统字体有可接受的字形差异 |
| 留白与布局 | 桌面左右分区，手机单列；减少手机说明区，离线入口可见；无横向溢出 |
| 配色与状态 | 奶油底、青绿主操作、危险动作区分；保留选中、禁用和回合反馈 |
| 图像与纹理 | 使用真实生成插画；资源加载成功；暖白棋盘、彩色棋块，未用整张样图替代交互 |
| 文案与语义 | 保留原有流程；输入、单选、按钮、details语义与焦点可用；不展示实现细节 |
| 棋盘几何 | 浏览器测量宽高相等；桌面完整可见，手机/平板无横向溢出 |

## 修正与复核历史

1. [已修复 P1] 新插画未加载：初次浏览器检查出现404。仅增加服务端静态白名单、发布资源清单；重新加载后图片正常。最终截图是修复后证据。
2. [已修复 P2] 手机大厅离线入口偏低：压缩介绍与装饰区域，最终手机截图显示主入口与离线入口。
3. [已修复 P2] 桌面高度受限时棋盘下沿被裁出视口：按可用视口高度限制棋盘宽度，同时保持正方形；1440×1024复核棋盘下沿约969 CSS px。
4. [已修复 P2] 两人/三人棋盘仍使用20×20的可访问名称：独立UI脚本依据已有DOM格子数更新名称；三人实测289格、标签17×17。规则按钮补上明确可访问名称。
5. [已修复显示问题] 原有离线结果缺失分数渲染为undefined：UI层显示“—”，已实测不再出现undefined；没有补算分数或改变胜负结果。

## 功能与代码边界

- game.js 与 server/game_engine.py 和改动前逐字节一致；共享棋块定义保持原样。
- server/app.py 的语法树在排除 STATIC_FILES 后与改动前一致；服务端改动只登记本次UI资源。
- 不替换游戏引擎，不改房间API、回合、胜负、禁手、落子校验或实时同步。
- 19项Python测试通过；JavaScript和Python语法检查通过。API测试初次被沙箱禁止监听本机端口，允许本机测试端口后通过；测试服务有一次连接关闭日志，不影响通过结果。
- 浏览器检查选块、旋转、翻面、取消、轻点定位与确认、鼠标双击落子、回合切换、2/3/4人棋盘、外观切换与自动收起、个人认输、全员结算与再来一盘。
- 单文件offline.html已重新生成。其核心游戏脚本与旧版本逐字节一致；新增UI脚本、CSS和图片均已内嵌。浏览器启动成功，无外部script标签，图片使用data URL；清洁离线预览控制台无error。
- UI辅助脚本只读取/修饰DOM，不发请求、不操作房间状态，不计算或修改游戏结果。

## 保留的边界与后续验证

- 这是基于设计的原项目UI整合，保留既有品牌标记、状态提示与真实交互，不是把静态导出页面直接替换进去。
- 真机触控、物理iPad拖动和公网部署尚未验证；鼠标与浏览器尺寸检查不能替代真机验证。
- 像素级字体统一属于后续P3优化；当前采用系统字体，未引入字体下载依赖。
- 原有离线分数数据缺失及其胜负计算未在本轮修复；只提供缺失值占位。原有在线退出浏览器确认保留，没有为了更换弹窗改动游戏控制流程。

final result: passed

此结果限定于上述UI实现、浏览器证据和核心逻辑未变的检查，不代表所有现有游戏问题已解决。
