# AGENTS.md

本文件适用于角域（Blokus）项目。遵守上级目录的项目路由和开发要求；在本仓库中执行读取、编辑、测试与 Git 操作。开始前阅读 `README.md`、检查 `git status --short --branch`，保留已有改动。文本文件末尾不增加空行。

## 已确认的页面设计方向

用户于 2026-09-07 确认「奶油潮玩」修订稿中的「角域 · 奶油拼趣」。对应设计板为大厅与候场、三端对局、规则结算与状态。后续页面设计与 UI 实现默认沿用这一方向，不需要重新探索整套风格；用户的新要求优先。

与弈五共享奶油底色、留白和柔和青绿色操作按钮；角域保留彩色拼块的年轻感。浅原木棋盘是弈五的要求，角域使用暖白格盘，不直接复制五子棋视觉与规则。

这是设计目标，不代表当前代码已经实现。只修改当前任务涉及的页面，不因本文件存在而自动启动全站重设计、提交、推送或部署。

## 可复用的页面设计提示词

```text
为网页版对角棋「角域」设计或实现页面，沿用已确认的「奶油潮玩 / 奶油拼趣」方向。
整体年轻、轻松、温暖、有桌面拼块玩具的触感。使用奶油白底色、薄荷绿主操作，以及杏黄、珊瑚、淡紫棋块，文字清晰且颜色易区分。
棋盘是暖白哑光的正方形格盘，网线清楚，彩色多格骨牌具有轻微立体感和柔和边缘。棋盘不使用深色底或五子棋木纹，不混入黑白圆棋子。
以圆润无衬线字体为主；需要与弈五统一中国风时，可仅在标题使用现代宋体。最多两套字体，正文与操作保持清晰。
保留柔和圆角、少量阴影和充分留白。大厅、候场或结算可使用少量彩色拼块与浅木托盘装饰，对局棋盘和棋块库以辨识、选择和操作为优先。
聚焦真实功能：创建与加入房间、2/3/4人选择、候场、在线与离线对局、棋块选择与旋转翻面、规则、名次结算、再来一盘、退出与外观选择。
电脑棋盘在左、棋块库和操作在右；平板和手机棋盘在上、棋块库和操作在下。旋转、翻面、取消与确认放在同一区域。
2人使用14×14、3人17×17、4人20×20方格棋盘。所有格子为正方形，实际棋块和合法落点由规则决定，不按图片伪造布局。
每个页面突出一个主操作，不新增账户、商城、货币、全局排行榜、聊天或计时功能。
生成样图时注明目标视口，使用正视网页内容，不添加手机外壳、系统状态栏或透视变形；实现时以真实规则、可读性与交互正确性校正样图误差。
```

## 视觉与内容要求

- 建议色板：奶油白 `#FFF8EE`、墨色 `#2D3934`、薄荷 `#86CCBA`、杏黄 `#F2BE57`、珊瑚 `#EC998E`、淡紫 `#B6A0DC`。允许为可读性调整明暗；浅色按钮使用深色文字或加深底色。
- 玩家颜色、名称、棋块、座位、预览和结算使用一致的颜色映射；更换展示色或名称时不改变游戏内部玩家身份与规则。
- 正文以 14–16 CSS px 为基准，主要点击目标至少 44×44 CSS px。选中、当前回合、错误和已结束状态同时使用文字或图标，不仅靠颜色区分。
- 圆角约 16–18px，低阴影。优先用留白、对齐和分隔线组织信息，不给每一块待选骨牌套独立大卡片，不使用多层嵌套卡片。
- 拼块装饰集中在大厅、候场与结算。不得遮挡棋盘、预览或选块区域，不堆积竹叶、印章或仿古背景。
- 文案描述玩法和下一步操作，不在规则中展示服务器、同步协议等实现说明；不得显示 `undefined` 分数或虚构数据。

## 页面、浮层与状态覆盖

本项目是单页应用；人数、角色和模式复用页面，不为每种组合增加独立路由。设计相关页面时覆盖下表中适用的状态。

| 页面或浮层 | 必须保留的内容与状态 |
| --- | --- |
| 创建大厅 | 昵称、2/3/4人选择与对应棋盘尺寸、创建房间、按所选人数离线同屏对弈 |
| 加入大厅 | 昵称、六位字母数字房间号、加入；提交中、错误、房间满员等反馈 |
| 候场 | 按人数显示座位、空座/齐人、房主开始/访客等待、复制邀请、退出 |
| 对局 | 在线/离线、2/3/4人、当前玩家、剩余棋块、等待回合、已结束玩家 |
| 棋块操作 | 未选、选中、旋转、翻面、取消、待定位、合法/非法预览、可确认、提交中 |
| 个人结束 | 认输后禁用个人操作、其他玩家继续；全员结束后结算 |
| 规则 | 覆盖起始角、同色角接角且不边接边、异色可贴边、剩余方格结算、操作方式 |
| 结果 | 本局名次、剩余方格、真实得分、并列获胜；按人数显示行数 |
| 再来一盘 | 邀请、已接受人数、等待其他玩家、有人离开无法重开；离线直接重开 |
| 在线退出确认 | 继续对局/确认退出；说明退出视为认输，其他玩家继续 |
| 外观 | 保留木桌竞赛、电子竞技、纸艺极简三个现有主题入口 |
| 通用反馈 | 邀请复制/手动复制兜底、重连/恢复、房间失效/满员、非法落点、非自己回合、请求失败 |

## 交互与规则边界

- 房间号是六位字母数字代码，不按纯数字验证码处理。加入时人数由房间决定，不让加入页改变人数。
- 在线与离线均按人数使用 14×14、17×17、20×20 棋盘。离线文案写「按所选人数轮流操作」，不能一律写为操作四种颜色。
- 离线界面不显示房间号、邀请按钮或在线连接状态。`offline.html` 复用离线页面设计，不增加单文件专属信息架构。
- 第一步覆盖自己的起始角；后续同色棋块必须角接角且不能边接边；异色棋块可以边接边。形状来自真实棋块定义，不从生成图提取。
- 保留轻点定位、鼠标/触控拖动、桌面双击落子，以及旋转、翻面、取消与确认。修改 Pointer Events 时同时检查触控 Pointer Capture、拖动预览、释放与回合锁定。
- 滚动棋块库不能误落子，旋转和翻面不能意外滚动页面；尚未选块、非法落点、非自己回合、提交中和个人已结束时正确限制操作。
- 在线规则与房间状态由服务端决定，不能只在客户端绕过校验。多人模式的「认输并结束」只结束该玩家的后续回合，不能错误地结束整场比赛。
- 邀请按钮名称与真实复制内容一致：复制链接时称「复制邀请链接」，同时展示可选中的六位代码；复制失败给出手动复制方式。
- 结算突出本局结果，不扩展为全局排行榜。主要按钮为「邀请再来一盘」，「回到大厅」为次操作；等待状态显示已接受人数，有玩家离开时禁用重开。
- 设计稿中的「外观」浮层收纳现有主题按钮，默认主题按本稿细化，不删除其他主题。退出浮层是对现有浏览器确认的视觉替代，不改变退出语义。

## 响应式与可访问性

- 桌面参考 1440×1024：大厅左标题与小插画、右表单；对局棋盘在左、棋块库和操作在右。
- 平板竖屏参考 834×1194：棋盘在上，棋块库、选中预览与操作在下；横屏空间足够时采用左右布局。
- 手机参考 390×844：单列、缩小装饰，棋盘尽量用满可用宽度；棋块库可独立滚动，选中块与旋转/翻面/取消保持可见，确认区始终可达。输入法弹出后仍可滚动提交表单。
- 人数与设备无绑定关系，所有视口都要支持2/3/4人；设计板用不同人数展示布局，不代表手机只支持2人。
- 视口尺寸用于设计参考，不是固定宽高。所有棋盘与格子必须正方形，不通过拉伸格盘或缩小点击目标解决拥挤。
- 规则面板可滚动，短确认紧凑；保留可见键盘焦点、清晰按钮名称，模态框正确管理焦点并在关闭后恢复。

## 样图误差与实现检查

- 图片只约束视觉与信息层级。网格数量、比例、坐标、局面、小字和按钮主次出现错误时，按本文件和真实规则校正，不能照图硬编码。
- 样图中未连接已有同色棋块的中部预览不是合法落点：实际合法性由规则计算；非法预览显示错误并禁用确认。
- 规则图不能混入五子棋圆棋子或木棋盘标记。「异色可以贴边」正文解释玩法，不复制样图中混入的服务器说明。
- 移除离线样图中误画的房间代码；结算按实际得分渲染，示例玩家、房间号和分数不写死。不能将整张样图作为交互页面。
- 先通过 `rg` 定位 `index.html`、`styles.css`、`game.js` 和棋块定义，再作最小修改；纯视觉调整不更换框架或游戏引擎。
- 共享样式或交互变化同时检查在线、离线和单文件版本。影响 `offline.html` 时运行 `python3 tools/build_offline.py` 生成，不只手改生成文件，并确认它不依赖服务器请求或外部资源。
- 实际 UI 改动检查桌面、平板、手机及鼠标/触控路径，分别报告静态检查、自动测试与浏览器验证结果。
- 按改动范围运行 `python3 -m unittest discover -s tests -v`、`node --check game.js`、`python3 -m py_compile server/app.py server/game_engine.py`。仅修改本文件时做文档和 diff 检查，不必运行游戏测试。
- 收尾检查 `git status --short`、`git diff --check`。提交、推送与部署仅在用户明确要求时执行；生产子路径为 `/blokus/`，发布细节以当前 `README.md` 和 `deployment/` 为准。

## 可编辑设计稿与落地记录

- [Pixso 设计文件](https://pixso.net/app/design/EkiwN1Njlbxn043HnBJpVA)，位于 `icego.tech` 文件夹。
- [设计与视觉资源说明](docs/design/README.md)、[本地 UI 验证记录](design-qa.md)。
